export const metadata = {
  title: 'Gemini Excel AI Settings',
  description: 'Manage models and system instructions for Gemini custom functions.',
};

export default function TaskpaneLayout({ children }) {
  return children;
}
