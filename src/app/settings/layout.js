export const metadata = {
  title: 'Google Sheets Gemini Settings',
  description: 'Manage the saved prompt used by the Google Sheets Gemini custom function.',
};

export default function SettingsLayout({ children }) {
  return children;
}